# Simplified Dockerfile for local JAR deployment
# Use this when server resources are limited and full build fails
FROM eclipse-temurin:17-jre-alpine
WORKDIR /app
COPY app.jar /app/app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "/app/app.jar"]
