# Use relative URL when behind nginx proxy (cloudflare tunnel, etc.)
# nginx proxies /api/* to backend
VITE_API_URL=/api/v1/miniapp

# Bot username for Telegram Login Widget
VITE_BOT_USERNAME=chatAutoModerBot
