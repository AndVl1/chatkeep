# Twitch-интеграция и Gated Features

## Обзор

Реализация двух взаимосвязанных систем:
1. **Gated Features** — универсальная система закрытых фичей для чатов
2. **Twitch-интеграция** — первая gated feature: уведомления о стримах в Telegram-чаты

## Цели

- Дать сообществам стримеров получать уведомления о начале стримов в их Telegram-чате
- Помочь модераторам быстро узнавать о старте трансляции
- Создать универсальную архитектуру для будущих платных/закрытых фичей

## Критерии успеха

- Уведомление о старте стрима приходит в течение 2 минут
- Сообщение обновляется при смене игры/названия
- Таймлайн стрима сохраняется и отображается корректно
- Система gated features легко расширяема

---

## Gated Features

### Архитектура

- **Нейминг в коде**: `GatedFeature`, `isGated`, `hasFeature()`
- **Хранение**: таблица `chat_gated_features` (chat_id, feature_key, enabled, enabled_at, enabled_by)
- **Feature keys**: строковые идентификаторы (например, `twitch_integration`, `advanced_stats`)

### UI в Compose-админке

- Раздел "Дополнительные возможности" в настройках каждого чата
- Список всех доступных gated features с тогглами
- Для каждой фичи: название, описание, статус вкл/выкл

### Поведение в Mini App

- **Если фича выключена** — пункт меню полностью скрыт
- **Если включена** — пункт отображается и работает
- Никаких "заблокированных" состояний или upsell

### API

```
GET  /api/v1/chats/{chatId}/features          — список фичей и их статус
PUT  /api/v1/chats/{chatId}/features/{key}    — включить/выключить фичу
```

---

## Twitch-интеграция

### Основные параметры

| Параметр | Значение |
|----------|----------|
| Каналов на чат | 3-5 |
| Кто настраивает | Админы чата |
| Авторизация Twitch | Серверный токен приложения (без OAuth пользователя) |
| Отслеживание статуса | EventSub (старт/стоп) + polling (обновления) |

### Поиск каналов в Mini App

- **Поиск по названию**: autocomplete через Twitch API
- **Вставка URL**: парсинг twitch.tv/username
- Результат: аватар канала, название, статус (online/offline)

### Сообщение о стриме

#### Формат
- **Тип**: Фото с caption
- **Фото**: превью стрима (только при старте, не обновляется)
- **Кнопка**: Inline-кнопка "Смотреть" с ссылкой на канал

#### Содержимое caption

```
🔴 {streamer} начал стрим!

{title}

🎮 {game}
👥 {viewers} зрителей
⏱ {duration}

📋 Таймлайн:
00:00 - Valorant | Just warming up
01:30 - CS2 | Ranked games
02:15 - Just Chatting | Q&A with viewers
```

#### Переменные шаблона

| Переменная | Описание |
|------------|----------|
| `{streamer}` | Имя стримера |
| `{game}` | Текущая категория/игра |
| `{title}` | Название стрима |
| `{viewers}` | Количество зрителей |
| `{duration}` | Длительность стрима |

#### Кастомизация шаблона

- Админы могут настроить текст сообщения через Mini App
- Доступны все базовые переменные
- Таймлайн добавляется автоматически

### Таймлайн

- **Формат**: `HH:MM - Game | Title` (если изменились)
- **Триггеры записи**: смена игры ИЛИ смена title
- **Хранение**: отдельная таблица `stream_timeline_events`

### Жизненный цикл сообщения

1. **Стрим начался** (EventSub webhook):
   - Создать запись стрима в БД
   - Получить превью и информацию
   - Отправить сообщение с фото
   - Записать первое событие таймлайна

2. **Стрим идёт** (polling каждые N минут):
   - Проверить изменения game/title
   - Если изменилось — добавить в таймлайн
   - Обновить сообщение (editMessageCaption)

3. **Стрим завершился** (EventSub webhook):
   - Обновить статус: "Стрим завершён"
   - Добавить итоговую длительность
   - Сохранить стрим в историю

### Обработка ошибок

| Ситуация | Поведение |
|----------|-----------|
| Twitch API недоступен | Пропустить обновление, повторить позже |
| Не удалось обновить сообщение | Логировать, продолжить работу |
| Канал удалён/заблокирован | Уведомить админа в Compose-админке |

---

## Хранение данных

### Новые таблицы

#### chat_gated_features
```sql
chat_id         BIGINT NOT NULL
feature_key     VARCHAR(50) NOT NULL
enabled         BOOLEAN DEFAULT false
enabled_at      TIMESTAMP
enabled_by      BIGINT
PRIMARY KEY (chat_id, feature_key)
```

#### twitch_channel_subscriptions
```sql
id              SERIAL PRIMARY KEY
chat_id         BIGINT NOT NULL
twitch_channel_id VARCHAR(50) NOT NULL
twitch_login    VARCHAR(50) NOT NULL
display_name    VARCHAR(100)
created_at      TIMESTAMP
created_by      BIGINT
UNIQUE (chat_id, twitch_channel_id)
```

#### twitch_streams
```sql
id              SERIAL PRIMARY KEY
subscription_id INTEGER REFERENCES twitch_channel_subscriptions(id)
twitch_stream_id VARCHAR(50)
started_at      TIMESTAMP
ended_at        TIMESTAMP
telegram_message_id BIGINT
telegram_chat_id BIGINT
status          VARCHAR(20) -- live, ended
```

#### stream_timeline_events
```sql
id              SERIAL PRIMARY KEY
stream_id       INTEGER REFERENCES twitch_streams(id)
event_time      TIMESTAMP
stream_offset   INTERVAL -- время от начала стрима
game_name       VARCHAR(200)
stream_title    TEXT
```

---

## Интеграция с Twitch API

### Авторизация

- Client Credentials Flow (серверный токен)
- Токен обновляется автоматически
- Никакого OAuth пользователя

### EventSub webhooks

Подписки:
- `stream.online` — стрим начался
- `stream.offline` — стрим завершился

### Helix API endpoints

- `GET /users` — информация о канале по login
- `GET /search/channels` — поиск каналов
- `GET /streams` — текущий стрим (для polling)

---

## UI в Mini App

### Раздел настроек чата

Если Twitch-интеграция включена, появляется пункт "Twitch-уведомления".

### Экран настройки Twitch

1. **Список подключённых каналов** (до 5)
   - Аватар, название, статус online/offline
   - Кнопка удаления

2. **Добавление канала**
   - Поле поиска с autocomplete
   - Возможность вставить URL
   - Результаты: аватар, название, кликабельно для добавления

3. **Настройки уведомлений**
   - Редактор шаблона сообщения
   - Превью как будет выглядеть

---

## Приоритет реализации

Параллельная разработка:

**Поток 1: Gated Features**
- Модель данных и миграции
- Backend API
- UI в Compose-админке
- Интеграция в Mini App

**Поток 2: Twitch-интеграция**
- Twitch API клиент
- EventSub webhook handler
- Polling service
- Telegram-сообщения
- UI настройки в Mini App

---

## Открытые вопросы

_Нет открытых вопросов — все требования уточнены._

---

## Следующие шаги

1. Создать feature branch `feat/twitch-integration`
2. Реализовать миграции БД
3. Реализовать систему Gated Features (backend + admin UI)
4. Реализовать Twitch-интеграцию
5. Добавить UI в Mini App
6. Тестирование
7. Deploy
