# Twitch API Credentials
# DO NOT commit actual values - this is just an example file
# Copy this to .env and fill with your real credentials

# Get your Twitch application client ID from: https://dev.twitch.tv/console/apps
TWITCH_CLIENT_ID=your_client_id_here

# Get user access token with 'channel:manage:broadcast' scope
# Visit: https://id.twitch.tv/oauth2/authorize?client_id=YOUR_CLIENT_ID&redirect_uri=http://localhost&response_type=token&scope=channel:manage:broadcast
# After authorization, copy the access_token from the redirect URL
TWITCH_USER_TOKEN=your_user_token_here

# Your Twitch broadcaster ID (numeric user ID)
# Get it by running:
# curl -s -H 'Authorization: Bearer $TWITCH_USER_TOKEN' -H 'Client-Id: $TWITCH_CLIENT_ID' https://api.twitch.tv/helix/users | jq '.data[0].id'
TWITCH_BROADCASTER_ID=your_broadcaster_id_here

# Optional: Update interval in seconds (default: 20)
UPDATE_INTERVAL=20
